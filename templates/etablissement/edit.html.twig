{% extends 'base.html.twig' %}

{% block title %}Modifier {{ etablissement.nom }}{% endblock %}

{% block body %}
    <div class="d-flex justify-content-between align-items-center flex-wrap p-4 col-sm-10 col-md-8 col-lg-6 mx-auto">
        <h1>Modifier l'établissement</h1>
        <div class="d-flex" >
            {% if app.user %}

                    <a href="{{ path('etablissement_show', {'id': etablissement.id}) }}" class="btn mx-1 btn-primary-color ">
                        <i class="fas fa-arrow-left me-2"></i> Retour</a>

                    {{ include('etablissement/_delete_form.html.twig') }}

            {% endif %}
        </div>
    </div>

    {{ include('etablissement/_form.html.twig', {'button_label': 'Enregistrer'}) }}

{% endblock %}
