<form method="post" action="{{ path('etablissement_delete', {'id': etablissement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ etablissement.id) }}">
    <button class="btn btn-danger mx-1 "><i class="fas fa-trash-alt"></i></button>
</form>
