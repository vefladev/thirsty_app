{% form_theme form 'bootstrap_5_layout.html.twig' %}
{{ form_start(form) }}
<fieldset class="col-sm-10 col-md-8 col-lg-6 m-auto ">
    {{ form_widget(form) }}
    <button class="btn btn-primary">{{ button_label|default('Save') }}</button>
</fieldset>
{{ form_end(form) }}
