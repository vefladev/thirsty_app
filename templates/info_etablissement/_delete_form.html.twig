<form method="post" action="{{ path('info_etablissement_delete', {'id_etab': info_etablissement.etablissement.id, 'id': info_etablissement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ info_etablissement.id) }}">
	<button class="btn btn-danger mx-1 "><i class="fas fa-trash-alt"></i></button>
</form>
