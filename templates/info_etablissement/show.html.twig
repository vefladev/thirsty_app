{% extends 'base.html.twig' %}

{% block title %}InfoEtablissement
{% endblock %}

{% block body %}
	<h1>InfoEtablissement</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Horaires</th>
				<td>{{ info_etablissement.horaires }}</td>
			</tr>
			<tr>
				<th>Description</th>
				<td>{{ info_etablissement.description }}</td>
			</tr>
			<tr>
				<th>Menu</th>
				<td>{{ info_etablissement.menu }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('etablissement_index') }}">back to list</a>

	<a href="{{ path('info_etablissement_edit', {'id_etab': info_etablissement.etablissement.id, 'id': info_etablissement.id}) }}">edit</a>

	{{ include('info_etablissement/_delete_form.html.twig') }}
{% endblock %}
