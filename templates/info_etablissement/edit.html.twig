{% extends 'base.html.twig' %}

{% block title %}Modifier les infos sur {{ info_etablissement.etablissement.nom }}
{% endblock %}

{% block body %}
<div class="d-flex justify-content-between align-items-center flex-wrap p-4 w-75 mx-auto">
	<h1>Modifier les infos sur {{ info_etablissement.etablissement.nom }}</h1>
	<div class="d-flex" >
	<a href="{{ path('info_etablissement_show', {'id_etab': info_etablissement.etablissement.id, 'id': info_etablissement.id}) }}" class="btn mx-1 btn-primary-color ">
		<i class="fas fa-arrow-left me-2"></i> Retour</a>
		{{ include('info_etablissement/_delete_form.html.twig') }}
	</div>
</div>
	{{ include('info_etablissement/_form.html.twig', {'button_label': 'Enregistrer'}) }}

{% endblock %}
