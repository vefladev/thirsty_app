{{ form_start(form) }}
<fieldset class="col-sm-10 col-md-8 col-lg-6 m-auto p-4 form-wrapper">
    {{ form_widget(form) }}

    <div class="d-grid gap-2 col-6 mx-auto mt-4">
        <button class="btn btn-primary-color">{{ button_label|default('Enregistrer') }}</button>
    </div>
</fieldset>
{{ form_end(form) }}
